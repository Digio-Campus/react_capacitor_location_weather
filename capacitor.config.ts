import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.react_capacitor_location_weather',
  appName: 'react_capacitor_location_weather',
  webDir: 'build'
};

export default config;
